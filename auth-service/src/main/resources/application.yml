spring:
  application:
    name: auth
server:
  port: 5000
logging:
  level:
    org:
      springframework:
        security: DEBUG
eureka:
  client:
    service-url:
      defaultZone: http://localhost:5001/eureka/
